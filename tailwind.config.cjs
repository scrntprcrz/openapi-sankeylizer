/** @type {import('tailwindcss').Config} */
module.exports = {
	content: ["./index.html", "./src/**/*.{js,jsx}"],
	theme: {
		extend: {
			/*  colors: {
        app: "rgb(var(--app-rgb) / <alpha-value>)", // MOD
        panel: "rgb(var(--panel-rgb) / <alpha-value>)", // MOD
        surface: "rgb(var(--surface-rgb) / <alpha-value>)", // MOD
        sep: "rgb(var(--sep-rgb) / <alpha-value>)", // MOD
        fore: "rgb(var(--fore-rgb) / <alpha-value>)", // MOD
        muted: "rgb(var(--muted-rgb) / <alpha-value>)", // MOD
        accent: "rgb(var(--accent-rgb) / <alpha-value>)", // MOD
      },*/
		},
	},
	plugins: [],
};
